<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/cardViewMenuItem"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_gravity="center"
    android:layout_margin="10dp"
    app:cardCornerRadius="15dp"
    app:cardBackgroundColor="@color/shadowBackgroundTop"
    app:cardElevation="10dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/shadowBackgroundTop"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/cardViewMenuItemImage"
            android:layout_width="125dp"
            android:layout_height="80dp"
            android:layout_gravity="center"
            android:scaleType="fitCenter"
            android:layout_margin="4dp"
            android:background="@color/shadowBackgroundTop"
            android:contentDescription="menu item image"
            android:src="@mipmap/ic_launcher" />

        <TextView
            android:id="@+id/cardViewMenuItemText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="4dp"
            android:text="Darshan"
            android:background="@color/shadowBackgroundTop"
            android:textColor="@color/black"
            android:textAlignment="center" />

    </LinearLayout>

</androidx.cardview.widget.CardView>